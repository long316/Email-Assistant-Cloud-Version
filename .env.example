# Copy this file to .env and adjust values for your environment

# API key required by write endpoints (POST/PUT/DELETE). Leave empty to disable.
API_KEY=your_api_key_here

#############################################
# Database DSN (PyMySQL). Examples
# Local/MySQL:
# DATABASE_URL=mysql+pymysql://user:password@127.0.0.1:3306/email_assistant?charset=utf8mb4
# Docker compose (external MySQL host shown as placeholder):
# DATABASE_URL=mysql+pymysql://user:password@mysql:3306/email_assistant?charset=utf8mb4
DATABASE_URL=

# Absolute files root path for templates, images (pics) and attachments
# If not set, defaults to <project_root>/files
# Windows example:
# FILES_ROOT=D:\\Email Assistant\\files
# Linux example:
# FILES_ROOT=/var/app/files
FILES_ROOT=D:\\Email Assistant\\files

# ===============================
# Email Assistant - .env template
# Fill values per your environment
# ===============================

# Auth flow: 'web' for server OAuth, 'desktop' for local testing
AUTH_FLOW=web

# Path to Google OAuth client JSON file
# Local example: credentials.json
# Docker example: /secrets/credentials.json (mounted read-only)
GOOGLE_OAUTH_CLIENT_JSON=credentials.json

# OAuth redirect URI (must match GCP console)
# Local example (no reverse proxy):
# OAUTH_REDIRECT_URL=http://127.0.0.1:5000/oauth/google/callback
# Production behind Caddy with path prefix /email-assistant:
# OAUTH_REDIRECT_URL=https://your-domain.com/email-assistant/oauth/google/callback
OAUTH_REDIRECT_URL=http://127.0.0.1:5000/oauth/google/callback


# Optional encryption key (reserved, not required now)
ENCRYPTION_KEY=


# OAuth return_url controls
# Comma-separated allowed hosts for return_url (exact host or suffix starting with a dot)
# e.g. crm.example.com,admin.example.com or .example.com
ALLOWED_RETURN_URL_HOSTS=localhost,127.0.0.1
# Fallback redirect when return_url is missing/invalid (optional)
DEFAULT_RETURN_URL=
# Allow localhost/127.0.0.1 in return_url during local dev
ALLOW_DEV_LOCALHOST=true
